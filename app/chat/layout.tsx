import type { ReactNode } from "react";

export const metadata = {
  title: "Prism Health — Chat",
  description: "Explore your health with Prism's AI health agent",
};

export default function ChatLayout({ children }: { children: ReactNode }) {
  return children;
}
